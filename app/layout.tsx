export const metadata = { title: "SwitchClaude — Switch Claude accounts in 2 sec", description: "Menubar switcher for Claude Desktop. One-time $9.99 per extra account." }
export default function RootLayout({children}:{children:React.ReactNode}){ return <html lang="en"><body className="bg-white text-zinc-900 antialiased">{children}</body></html> }
